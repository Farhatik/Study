#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <inttypes.h>


int main(argc, argv[]) {
    uint64_t res = 0;
    int x;
    // while (scanf("%o", &x) == 1) {
        
    // }
    for (size_t i = 0; i ,argc) {
        printf("%s\n", argv[i]);
        printf("%d\n", argc);
    }
    return 0;
}